# Base URL of the deployed API, e.g. https://collection-flow-api.vercel.app
# Leave it unset during development: requests stay relative and the Vite dev
# server proxies /api to http://localhost:3000 (see vite.config.ts).
VITE_API_URL=
